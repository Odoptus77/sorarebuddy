# Secrets / local config — NEVER commit
.env
.env.local
.env.*.local
*.key
secrets/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# Backend cache + SofaScore team-id cache (runtime data)
backend/cache/
sofa_teams.json

# Xcode / Swift — the .xcodeproj is generated locally from ios/project.yml
ios/SorareBuddy.xcodeproj/
ios/build/
ios/**/xcuserdata/
*.xcuserstate
DerivedData/

# OS / editor
.DS_Store
.idea/
.vscode/

# Accepted lineup baseline for the 5h sync check (account data, do not commit)
baseline_lineup.json

# Runtime output artifacts (contain account data — do not commit; see CLAUDE.md conventions)
club.json
rewards.json
lineups.json
dashboard.html
*.csv
